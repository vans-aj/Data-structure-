string removeConsecutiveCharacter(string& s) {
        int n = s.size();
        string str = "";
        for (int i =0; i<n-1; i++){
            if (s[i]==s[i+1]) continue;
            str += s[i];
        }
        str += s[n-1];
        return str;
    }
};
